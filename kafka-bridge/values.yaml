# Default values for kafka-bridge.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

Replicas: 1
KafkaServers: kafka-kafka-instance-kafka-bootstrap:9092
Port: 8080
